package hackmaster20.persistence;

import java.lang.Enum;
/**
 * Created by Owner on 1/29/2018.
 */

public class cardDatabase {
    // make some card specs
    public static enum CardType { Attack,Defense,Upgrade };
    public static enum CardResource { CryptoCoin,CPU,Botnet };
    public static enum CardResourceRate { CryptoRate, CPURate, BotnetRate }


    /*
    public static CardClass cardBase() {
        // make some cards where you set all the specs
        CardClass cards[10];

        CardClass tempCard = new CardClass(name, new Resource(), );

    }
         * */
}
